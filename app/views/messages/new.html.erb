<div id="message-info">
  <h3>Enter your details</h3>
  <%= form_for @message, remote: true do |f| %>
    <div class="form-group">
      <div id="name-header">Name: <%= f.text_field :name %>  (not to exceed 50 characters)<br></div>
      <div id="number-header">Number: <%= f.text_field :number %>  (omit spaces, hyphens)<br></div>
      <div id="content-header">Content: <%= f.text_field :content %>  (not to exceed 122 characters)<br></div>
    </div>
    <%= f.submit "Send message", class: "btn btn-primary", id: 'send-message-link' %>
  <% end %>
</div>
<div id="status-box" class="alert alert-success">
  <p id="status-message">Status: Haven’t done anything yet</p>
</div>
